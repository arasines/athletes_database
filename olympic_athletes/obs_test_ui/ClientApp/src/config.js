const config = {
  defaultPath: '/',
  defaultId: 'main-dahsboard',
  fontFamily: `'Inter', 'Roboto', sans-serif`,
  borderRadius: 8,
  drawerWidth: 300,
  sidebarWidth: 300,
  gridSpacing: 2,
};

export default config;
